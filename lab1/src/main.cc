#include <iostream>

#include "generator.h"
#include "single_layer_perceptron.h"

int main(int argc, char* argv[]) {
  const auto& data_supplier = lab1::DataSupplier::GetInstance();
  const auto perceptron = lab1::SingleLayerPerceptron();
}
